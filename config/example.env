# Copy to config/.env (DO NOT COMMIT). Populate only on your machine.
# Do NOT paste secrets into chat. Use a local secret manager if possible.

# AWS
AWS_PROFILE=
AWS_REGION=
S3_BUCKET_DATA_LANDING=
GLUE_ROLE_ARN=

# If you must use static keys, export them in your shell only:
# export AWS_ACCESS_KEY_ID=...
# export AWS_SECRET_ACCESS_KEY=...

# PATCHIT runner
PATCHIT_CMD=
OUTPUT_DIR=
